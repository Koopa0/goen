// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: query.sql

package db

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createContactMessage = `-- name: CreateContactMessage :one
INSERT INTO contact_messages (name, email, subject, order_ref, message)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, created_at
`

type CreateContactMessageParams struct {
	Name     string
	Email    string
	Subject  string
	OrderRef pgtype.Text
	Message  string
}

type CreateContactMessageRow struct {
	ID        uuid.UUID
	CreatedAt time.Time
}

func (q *Queries) CreateContactMessage(ctx context.Context, arg CreateContactMessageParams) (CreateContactMessageRow, error) {
	row := q.db.QueryRow(ctx, createContactMessage,
		arg.Name,
		arg.Email,
		arg.Subject,
		arg.OrderRef,
		arg.Message,
	)
	var i CreateContactMessageRow
	err := row.Scan(&i.ID, &i.CreatedAt)
	return i, err
}

const subscribeNewsletter = `-- name: SubscribeNewsletter :exec
INSERT INTO newsletter_subscribers (email)
VALUES ($1)
ON CONFLICT (lower(email)) DO UPDATE SET unsubscribed_at = NULL
`

// Re-subscribing an address that previously opted out clears the opt-out
// rather than failing, so the visitor sees the same confirmation either way.
func (q *Queries) SubscribeNewsletter(ctx context.Context, email string) error {
	_, err := q.db.Exec(ctx, subscribeNewsletter, email)
	return err
}
